# Cursor family grok / architecture — 2026-08-17

**Family:** Cursor (this run). **Owner:** Keith BBF. **Orchestrator:** GbQA.
**Corpus:** GitHub floor `keithbbf-gif/bts-mesh` at `fb6c53d78f7b905ed74c914565fd8b0964abdf29`
("full mesh snapshot 2026-08-16"). **Not** the recovered 296-file hashed corpus.
**This brief cites only paths that exist in that snapshot.** Missing things stay **UNKNOWN**.
**Status:** draft PR only. Do not merge. Do not apply KDash PR #3 or any other open PR.

Counted on disk at grok time: **251 files** under `BTS_MESH/` + `ROLD/` (119 `.py` in `BTS_MESH/`).
Tree is two folders plus `README.md`. That is the whole GHX program snapshot.

---

## 0. What this snapshot is, and is not

| Present here | Pointed at, **absent** from this repo |
|---|---|
| `BTS_MESH/` modules, ledgers, dashboards, bats | `V:\Ai\BFast\` (`bfast.py`, `roots.json`) — `ROLD/rails.toml` `[bfast]` |
| `ROLD/rails.toml` (machine registry) | `V:\Ai\BU.MD` (boot pointer, `ROLD/RULES.md` R5) |
| `ROLD/{RULES,RAILS,ROUTINES,SCARS,GLOSSARY}.md` | `V:\Research4\Ai\00_MESH_CHARTER.md` |
| `ROLD/{verify_pointers,verify_conf,scars,corrections}.py` | `V:\Ai\Legal\` (case tree — do not touch) |
| `ROLD/{registries,corrections,archive}.toml` | `V:\Research4\.secrets\` (do not touch) |
| `BTS_MESH/bts_identity.py` (`MESH_ID = "KMesh"`) | `QA_BRIEF.md` (named in `README.md`; generated by `qa_handoff.py`, not checked in) |
| | `V:\A` as a path — **zero hits**. **HOLD.** See §6. |
| | `Crucible` — **zero hits**. See §7. |
| | `queen`, `two-universe`, `hub-through` — **zero hits**. Do not reconstruct. |

`README.md` is a publisher blurb. It is not the architecture. `ROLD/rails.toml` is.

`ROLD/RAILS.md` is a human twin of constants that `rails.toml` already owns. The toml header
forbids a generated markdown twin. Treat `RAILS.md` as **drift-prone prose**, not as the
registry (`SCARS` TOOLS_INDEX drift; `bts_kdash_feed.py` docstring).

---

## 1. Nodes vs rails vs surfaces

`ROLD/GLOSSARY.md` states the split. `ROLD/rails.toml` is the live table. A thing is only one of these.

### Nodes — minds you can address (`[[node]]`)

| id | File that talks to it | Kind in toml | What it actually is |
|---|---|---|---|
| **CoW** | `BTS_MESH/bts_claude.py` (ledger only) | cli+mcp | Claude Code / Cowork. Slowest measured CLI (`cli-claude` 15,647 ms). |
| **GW** | `BTS_MESH/bts_gw.py` | cli+api | Grok Build `grok-build-0.1`. Same xAI key as SGH. |
| **SGH** | `BTS_MESH/bts_sgh.py` | api | Grok 4.5. Node **and** xAI rail in one module. |
| **GEM** | `BTS_MESH/bts_gem.py` | cli+api | Gemini **node**. Consumes two Google rails. **Not Vertex.** |
| **CoPG** | `BTS_MESH/bts_cop.py` | cli+mcp | GitHub Copilot CLI. Distinct from CoP365 (`ROLD/RULES.md` R13). |
| **OA** | `BTS_MESH/bts_oa.py` + `bts_oa_api.py` | cli+mcp | Two **lanes**, one node. Codex plan ≠ metered API. |
| **CoP365** | none callable | observation-only | **Registered, not counted.** No MCP. Not a node. |

`BTS_MESH/bts_kdash_feed.py` must render nodes from `rails.toml`, not from `bench.json`.
`federation_ready()` in `bts_identity.py` is **False** with five blockers. Do not report federation live.

### Rails — how a call leaves the box (`[[rail]]`)

| name | Module | Kind | Notes from this snapshot |
|---|---|---|---|
| `crossref` | (HTTP in `verify_citations.py`) | api | DOI truth. Never ask a model for a DOI (`RULES.md` R9). |
| `itc` | rclone / publish bats | api | Public R2 mirror `https://ai.dchambers.com`. One-way. |
| `xai` | `bts_sgh.py`, `bts_gw.py` | api | Shared monthly ceiling. Ledger `sgh_spend.json` / `gw_spend.json`. |
| **`vertex`** | **`bts_vertex.py`** | **api** | **GEM credit rail. Not a node. See §2.** |
| `bfast` | pointed: `V:\Ai\BFast\bfast.py` | mcp | 13 tools. Implementation **absent** here; semantics in `bts_tools.py`. |
| `bfast-handoff` | same server, GDX root | mcp | Measures local Drive cache, **not** WAN arrival. |
| `cli-copilot` | `mesh_fanout.py` `_copg_argv` | cli | npm `.cmd` shim — launch via `cmd /c`. |
| `cli-grok` | grok CLI | cli | Cheap (subscription). Mid-pack latency. |
| `cli-gemini` | `bts_gemini_cli.py` | cli | **REGISTERED, not LIVE** — binary on PATH, no prompt returned to file (`conf=U`). |
| `cli-claude` | Claude Code | cli | Prepaid allotment. Do not default-loop it (C-01). |
| `gemini-dom` | Chrome / Keith session | dom | Thinking is free on the web app. API bills thinking at output rate. |
| `openai-codex` | Codex CLI | cli+mcp | Prepaid ChatGPT plan. 272k ceiling. |
| `openai-api` | `bts_oa_api.py` | api | Metered. 1.05M in. Only lane that can hold the ~422k war-game record. |

### Surfaces — storage with one job (`[[surface]]`)

Axis in `rails.toml` is **reach without a human**, not disk speed.

| name | Job in toml | Letter today (implementation, not identity) |
|---|---|---|
| `V: Ai` | PRIMARY shared surface | `V:\Ai` |
| GDX | Provisional WAN / rendezvous | `X:\My Drive\BTS_SGH_Handoff` (`bts_gdx.py`) |
| ITC | Public mirror, publish-only | `https://ai.dchambers.com` |
| ODX | Archive / private backup | `D:\ODX\OneDrive\BTS_ODX` |
| JDX | Candidate. **Nothing depends on it.** | joanna Drive, not mounted |
| G: | Recovery **mirror**, not a surface | USB SABRENT. Faulted. Not a NAS. |
| GHX | This private repo | `keithbbf-gif/bts-mesh` (`qa_handoff.py`) |

`bts_identity.SURFACES = ["ITC", "GDX", "ODX", "TB1", "LOCAL"]` — a **second list**. TB1 is
candidate-only. Two lists is the defect `bts_kdash_feed.py` exists to kill. **Borrow the
toml table; do not keep a parallel constant.**

---

## 2. Vertex is a GEM credit rail, not a model node

`BTS_MESH/bts_vertex.py` opens: *"VERTEX rail. Gemini 2.5 Pro on the $300 Google Cloud credit."*
It is separate from `bts_gem.py` because the credit **cannot** pay AI Studio. Vertex / Agent
Platform is the door the credit opens.

`bts_gem.py` is the **node**. It may call Vertex (`via="vertex"`) or AI Studio. Callers
address **GEM**, not Vertex. `mesh_fanout.py` imports `bts_vertex` as GEM's paid leg.
KDash must not grow a "Vertex" node next to GEM (`bts_kdash_feed.py` already warns
`bench.json` did this).

Joanna.bbf **holds the credit**. That is a billing identity, not a mind.

```
GEM  (node)  ──┬── AI Studio rail   (keith.bbf keys / free-tier path — BROKEN in this record)
               └── vertex rail      (joanna.bbf $300, expires 2026-10-13, bts_vertex.py)
```

`cli-gemini` and `gemini-dom` are more GEM rails, still not nodes.

---

## 3. Identities — default login vs credit key

`BTS_MESH/bts_identity.py`: `OWNER_MAIL = "keith.bbf@gmail.com"`. That is the default login.

| Account | Role in this snapshot | Do |
|---|---|---|
| **keith.bbf@gmail.com** | Default operator. Google AI Plus (`rails.toml` `[[subscription]]`). GDX inference (account on GDX block is still **UNKNOWN** — three inferences, not a measurement). xAI, Claude, CoPG, Codex, OpenAI API. Intended GEM **paid API** / AI Studio project `475026348209`. | Login here. Pay here. Mount GDX here. Create/repair GEM API keys here. |
| **joanna.bbf@gmail.com** | Vertex **credit only**. Project `project-5a33f910-1251-4d6a-bf9`, billing `010E47-824B53-7202F5`. Credit ~$286.07 / expiry **2026-10-13** (two console readings). | **Read and use the existing credit. Never register, upgrade, or click Activate.** |
| dchamberss@gmail.com | xAI billing (discrepancy block) | Label findings by account. |
| Ranny.bbf@gmail.com | JMesh / Jack family account (2026-08-16 note) | Naming hazard only. Does not move the Vertex credit. |

Keith, 2026-08-12, in `rails.toml` `[[rail]] vertex` and `bts_gem.py`:
*"Activating that account destroys the free credits, and we don't need to."*
**Nobody clicks Activate on joanna.bbf.** Not to unlock, raise quota, or fix a 403.

C-31 (`ROLD/corrections.toml`): "joanna holds the only Google API key" was a **structural lie**.
Joanna holds the **credit**. keith.bbf already had AI Studio keys. Conflating key with credit
steered the October plan. Do not repeat it.

JDX must not become a backup surface while it also holds the credit (`rails.toml` JDX `owed`).

---

## 4. One write path

This snapshot has **many** writers. That is the defect to adapt away from, not to copy.

| Writer | Path it writes | Class |
|---|---|---|
| `bts_tools.write/append/edit` | Guarded roots: `V:\Research4`, `/sessions`, `/tmp`. SHA-256 read-back. | Tool layer BFast is supposed to call |
| `bts_bus.atomic_write` | temp → fsync → `os.replace` | **The primitive to keep** |
| `bts_state.emit` | `MESH_STATE.events.jsonl` + `mesh_state.json` | Event log; projection is disposable |
| `bts_runner.py` | `V:\Ai\_queue\{running,done,failed,logs}` + `runner_ledger.jsonl` | Native job claim-by-rename |
| Per-rail ledgers | `sgh_spend.json`, `vertex_usage.json`, `oa_api_spend.json`, … | Meter files beside the modules |
| `mesh_fanout.py` | default `V:\Ai\Legal\MESH\{tag}__{NODE}.md` | Parallel returns (Legal — do not extend) |
| `bts_gdx.py` | Drive folder `1aiMjVZfFtiBGNAlkTwAYiq1Pvuz2F6hA` | WAN rendezvous |
| `bts_kdash_feed.py` | `dash.json` | Derived only — no human edits |
| `bts_surfaces.py` | `surfaces.json` | Measurement dump |
| `rail_check.py` | `RAIL_HEALTH_<date>.md` + ODX copy | Monitor artifact |

**Borrow:** `bts_bus.atomic_write` + `bts_tools._guard` (prefix, not substring) + read-back hash
(`bts_tools.py` — this is why BFast is not a generic MCP filesystem).

**Build (do not xerox the sprawl):** one durable write API. Callers ask a **share-root role**
and a relative path. The implementation picks the mount. Ledgers stay next to their rail
module (one file per pool). Fan-out, queue, and GDX are *destinations*, not second primitives.

BFast colon roots in `rails.toml` (`ai:`, `research4:`, `handoff:`) are the right *shape*.
The root-shadowing scar (a node wrote `research4:handoff/` when it meant `handoff:`) is why
relative first-segments must be refused.

---

## 5. Share-root roles, not drive-letter religion

`bts_paths.py` already says it: *"Callers ask for a ROLE … never a letter. The letter is an
implementation detail that has now changed twice."* Helpers: `ai()`, `mesh()`, `archive()`,
`working()`, `secrets()`.

Then the same file hardcodes `_WIN = [r"V:\Research4"]` and `bts_tools._ROOTS` repeats the
letter. `RAILS.md` is a drive-letter catechism. That is how `D:\Research2` was hardcoded
1,961 times and a migration sweep ate `bts_paths.py` itself (S-30).

**Adapt the role idea. Do not xerox the V:\Research4 ladder as the OS.**

Proposed roles (names from this snapshot, letters as *current binding only*):

| Role | Current binding | Job |
|---|---|---|
| `ai` | `V:\Ai` | Primary shared surface. AI-to-AI. |
| `research` | `V:\Research4` | Dissertation / published subtree. One stream's tree, not the root. |
| `handoff` | GDX folder | Provisional WAN. Confirm account before treating as keith.bbf. |
| `archive` | ODX | Private backup. CoP365 reader. |
| `queue` | `V:\Ai\_queue` | Native runner inbox. |
| `secrets` | sibling of `Ai\`, never inside it | Keys by **account + vendor**, not by person. |
| `hold` | **`V:\A`** | **HOLD. See §6.** |
| `ghx` | this repo | Code review surface. Allowlist only (`qa_handoff.py`). |

G: is a recovery mirror. ITC is dissemination. Neither is a share-root for mesh writes.

---

## 6. `V:\A` is HOLD

**UNKNOWN in this snapshot.** Grep found `V:\Ai` everywhere and `V:\A` nowhere.

Until a host measurement exists: do not mount it, write it, register it as GREEN, or invent
a job for it. A missing surface is **HOLD / UNKNOWN**, not an empty disk waiting to be used
(S-96 / `bts_drive_health.py`: a bridge that hides SMART reports UNKNOWN, never GREEN).

---

## 7. Crucible after a live mesh

**Crucible is UNKNOWN here** (zero hits). Do not design it in this PR.

What *is* here: `BTS_MESH/wargame.py` + `wargame_feed.py` + `wargame_bundle.py` — a **legal**
adversarial trial over Chambers v. Abraxas. That is case work. It is not a mesh crucible.
Do not generalize it into an OS.

`ROLD/RULES.md` R19 order: (1) all rails up, ledgered, on KDash without stale strings →
(2) phone + voice → (3) share with Jack, then Christina.
`bts_identity.federation_ready()` is False. **A crucible that assumes a live multi-node mesh
is fiction until (1) is true.** Build it after, or not at all in this brief.

---

## 8. Unknowns stay UNKNOWN, not green

Already enforced in this snapshot. Keep it. Do not "fix" it to look healthy.

| Mechanism | Path |
|---|---|
| `conf = "V"` needs `measured`; `conf = "U"` needs `owed` | `ROLD/verify_conf.py`, `rails.toml` header |
| Unknown opcode is a failure, never a skip | `ROLD/verify_pointers.py` |
| No SMART counters → UNKNOWN, not GREEN | `BTS_MESH/bts_drive_health.py` |
| Missing value is NOT MEASURED — never 0 | `BTS_MESH/maint_sweep.py` |
| Unmeasured latency renders UNMEASURED, not 0 | `BTS_MESH/bts_kdash_feed.py` |
| Presence ≠ return (S-100) | `cli-gemini` still `conf=U`; OA was REGISTERED until a file landed |
| Silence of `runner_ledger.jsonl` ≠ runner alive | `runner_doctor.py`; queue-and-unclaimed is the signal |
| Stale `state=` strings on KDash | monitors in `rails.toml` (Rail Check date, R2 Publish, Publish Watch) |

---

## 9. Failure classes (from `ROLD/scars.jsonl`, 147 scars)

Machine tags, not invented categories:

| Tag | n | Load-bearing IDs | What it means for a new design |
|---|---|---|---|
| `mount` | 49 | S-05..S-07, S-28, S-30, S-31 | Sandbox read is not evidence. Never `cp` through FUSE. |
| `auth` | 43 | S-04, S-17, S-36, S-97, S-99 | State the **account** inside every gcloud/Google finding. |
| `silent` | 34 | S-10, S-25, S-64, S-133, S-142 | Exit 0 is not an artifact (C-29). |
| `measurement` | 33 | S-96, S-120, S-141 | Estimate ≠ cost. Unverified fact cannot downgrade measured (C-27). |
| `naming` | 30 | S-13, S-37, S-75, S-119, S-146 | GDX is X: not E:. UPS = spectroscopy, not a battery. |
| `fabrication` | 12 | S-33, S-34, S-55, S-130 | Cite the file, not context (C-08). |
| `control` | 11 | S-118, S-122, S-147 | A verifier never shown to fail is not a verifier (R11). |
| `encoding` | 9 | S-59, S-90, S-134, S-145 | No inline programs in shells (C-06). |
| `scheduler` | 3 | S-42, S-134, S-142 | Native vs Cowork schedulers are not interchangeable. |

Corrections `C-01`..`C-37` live in `ROLD/corrections.toml`. Highest mesh-design weight:
**C-01** (Claude solo / idle prepaid), **C-16** (verify the referent), **C-22** (report
measured confidence), **C-27**, **C-29**, **C-31**.

---

## 10. Adapt / borrow vs build

R14 (`RULES.md`): look outward before building; write the finding down. This section is that
finding **against this snapshot**, not against the wiped corpus we do not have.

### Borrow (keep the mechanism, maybe move the file)

- `ROLD/rails.toml` as the **only** machine registry (nodes / rails / surfaces / monitors / clocks).
- Confidence schema + `verify_conf.py` / `toml_guard.py` (S-118 duplicate-key parse death).
- Pointer operators + `verify_pointers.py` (unknown opcode = fail).
- `bts_identity.py`: software name `BTS_MESH` vs instance `KMesh`. One constant per peer.
- `bts_bus.atomic_write` + append-only signal log with per-reader cursor.
- Per-rail spend ledgers that **fail closed** when unreadable (`bts_sgh`, `bts_oa_api`).
- `bts_kdash_feed.py` rule: render the registry; never keep a second node list.
- `qa_handoff.py` allowlist + live-key abort (including GitHub token shapes).
- `route_gate.py` refusals: no delegate without a prepared feed; no unscoped negative claim.
- `bts_deadline.py`: empty file → `RED — TIMED OUT`, not success-by-silence.
- Sandbox facts: 45 s cap, background dies with the call, Windows path literals become directory names (`rails.toml` `[sandbox]`).

### Adapt (same job, different shape — do not xerox)

- **Role resolver** from `bts_paths.py` — drop the religion that the research tree *is* the OS.
- **GEM node + Vertex rail** — keep the split; default **login and new GEM paid API to keith.bbf**; Joanna remains credit-use-only.
- **BFast colon roots** — keep the role prefix; do not rebuild `V:\Research4\Ai\BTS_MCP\` here (implementation is absent).
- **Native queue runner** — keep claim-by-rename; wire `runner_doctor.py --check` into rail_check (owed in toml).
- **DOM + API both** (R1a) — thinking-heavy → `gemini-dom` / prepaid CLIs; short scriptable → API. API is fallback except while the dated Vertex credit must be burned (until 2026-10-13).
- **KDash** — keep as localhost `bts_serve.py` + `jack_command.html` on `127.0.0.1:8765`. Do not apply open KDash PRs in this run.

### Do not xerox (wiped or absent designs)

| Named design | In this snapshot? | Ruling |
|---|---|---|
| **Research4 as the universe** | Paths everywhere; tree not here | Research is **one share-root**, not the mesh home. Do not re-center the OS on `V:\Research4`. |
| **TidyUP / TidyUP2 as the OS** | `ROLD/ROUTINES.md`, `tidyup2_checks.py`, `tidyup2_full.py`, many `_tidy_*.bat` | Adversarial checks are useful. A 79 KB monolith + ten-check ritual that certifies pre-edit state (S-122) is not an architecture. Do not rebuild TidyUP2. Do not touch TidyUP bats. |
| **two-universe** | **UNKNOWN** (zero hits) | Do not invent a dual-universe topology to replace something we cannot read. |
| **hub-through-queen** | **UNKNOWN** (zero hits) | Do not invent a queen/hub. This mesh is nodes + rails + surfaces + one bus. |
| Federation / JMesh LAN | Planned in `bts_identity.py` | Five blockers. Meeting point does not exist. G: is USB. |
| JDX as archive | Candidate only | Concentration risk: credit + key + backup on one account. |

### Build (missing here; needed for a live mesh)

1. **One write path** over share-root roles (§4–§5).
2. **keith.bbf GEM paid API** proven to a file **before** 2026-10-13, labeled by account. The AI Studio path is recorded BROKEN (`[[node]] GEM`). Fixing it after the credit dies means diagnosing with no working rail.
3. **GDX account measurement** — Drive-for-Desktop settings panel, then write the email into `rails.toml`. Until then GDX holder stays UNKNOWN.
4. **`cli-gemini` first return to a file** before `conf=V` (S-100).
5. Monitor liveness from **artifact mtime**, not hand-copied `measured=` dates.
6. Phone + voice client (R19 item 2) — a client with **no drive letters**. Same problem as a peer. Do not solve it twice.
7. **Crucible: after (1), not instead of (1).**

Do not build: Joanna billing changes, Legal writers, publish-script edits, `.env` / secret files, TidyUP bats.

---

## 11. Module map (this snapshot only)

### Control plane
`bts.py` CLI · `bts_orchestrator.py` (conductor skeleton; `launch_session` stub) ·
`bts_conductor.py` (dead-letter, stale reap) · `bts_node.py` · `bts_registry.py` ·
`bts_route.py` · `route_gate.py` · `bts_launch.py` · `bts_gate.py` · `bts_discovery.py` ·
`bts_doctor.py` · `bts_policy.py` · `bts_meters.py`

### Rails / nodes
`bts_sgh.py` · `bts_gw.py` · `bts_gem.py` · `bts_vertex.py` · `bts_gemini_cli.py` ·
`bts_gdx.py` · `bts_oa.py` · `bts_oa_api.py` · `bts_cop.py` · `bts_claude.py` ·
`bts_billing.py` (account state only — **no spend API exists**) · `bts_creditprobe.py` ·
`_verify_joanna_rails.py` · `_gem_force_vertex.py`

### Surfaces / health
`bts_surfaces.py` · `bts_capacity.py` · `bts_drive_health.py` · `bts_drivebench.py` ·
`bench_drive.py` · `disk_guard.py` · `rail_check.py` · `bts_health.py` · `maint_sweep.py` ·
`bts_serve.py` · `bts_kdash_feed.py` · `bts_chanbench.py`

### Write / run
`bts_tools.py` · `bts_bus.py` · `bts_state.py` · `bts_runner.py` · `runner_doctor.py` ·
`runner_repair.py` · `bts_watcher.py` · `bts_watchdog.py` · `bts_deadline.py` ·
`mesh_fanout.py` · `bts_mcp.py` (schema for gdx/signal/gate — not BFast)

### Governance (ROLD)
`rails.toml` · `registries.toml` · `corrections.toml` · `archive.toml` · `scars.jsonl` ·
`verify_pointers.py` · `verify_conf.py` · `scars.py` · `corrections.py` ·
`RULES.md` R1–R20 · `ROUTINES.md` · `SCARS.md` · `SESSION_HANDOFF_SOP.md`

### Present, out of scope for this brief
Legal/war-game (`wargame*.py`, `rfa_review.py`, `sweep_corpus.py`, `STREAM_LEGAL.md`) —
do not extend. TidyUP / publish / `_migrate_*` / `_tidy_*.bat` — do not touch.
`gemini_cli_env.json` holds project/location/Vertex flags only (no API key in-file).

---

## 12. UNKNOWN ledger (do not promote)

- Holder email of GDX / `X:` — inferred keith.bbf, **not measured**.
- Whether Vertex Agent Platform is credit-eligible — `[G]` in `bts_vertex.py`; console "Other savings" is the instrument.
- `cli-gemini` live return.
- keith.bbf AI Studio free/paid path — recorded broken; not re-proven in this checkout.
- `V:\A` existence, size, job — **HOLD**.
- Crucible — not in corpus.
- two-universe / hub-through-queen / queen — not in corpus.
- BFast on-disk (13 tools, `roots.json`) — pointed, not shipped.
- GDX WAN propagation latency — local cache 14.8 ms is the wrong quantity.
- ODX write drop 63.3 → 17.5 MB/s — one sample; daemon vs disk **UNKNOWN**.
- Keith SGH promo start date — may collide with 2026-10-13; unread (`[[discrepancy]]`).
- CoPG allowance — unpublished (`[[node]] CoPG`).
- Jack SuperGrok Heavy charge date — `conf=U`.
- Federation meeting point.
- Whether this checkout's ledgers match the live box — files are here; this agent did not spend.

---

## 13. Done-means for anyone continuing

A later pass is **not** licensed to: merge this PR, force-push `main`, apply other PRs,
edit Legal / secrets / `.env` / live-key ledgers / TidyUP bats / publish scripts,
register Vertex as a node, Activate joanna.bbf, or paint `V:\A` / Crucible / GDX-account green.

It **is** licensed to: keep `rails.toml` as the registry, address GEM not Vertex, default
keith.bbf for login and new GEM/GDX work, use Joanna's credit read-only until 2026-10-13,
write through one role-addressed path, and leave unknowns red.
